// =============================================================================
// main.rs - Tauri 应用主入口
// =============================================================================
// 说明：标准 Tauri 2.0 入口，仅调用 lib::run() 启动应用。
// 所有业务逻辑均在 lib.rs 中注册，便于在移动端 / 桌面端复用。
// =============================================================================

// 防止 Windows 下缺少控制台窗口（release 模式）
#![cfg_attr(not(debug_assertions), windows_subsystem = "windows")]

fn main() {
    flipper_ai_tutor_lib::run()
}
